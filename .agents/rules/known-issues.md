---
activation: always_on
---
# Known Issues — BrixSports
## Bugs Already Solved (Never Reintroduce)
2026-06-05 — Middleware path mismatch (BUG-001) — `pathname.startsWith('/admin')` did not cover `/api/admin/*` routes, so middleware ran but did nothing for API calls — Always match both `/admin` and `/api/admin` in the internal check, not just the matcher config.
2026-06-05 — Debug endpoint in production (BUG-003) — `src/app/api/auth/test/route.ts` had no auth gate and exposed user ID/role/email to any caller — Never leave test/debug routes in production; delete them before any deploy.
2026-06-05 — Admin API routes with no auth (BUG-002) — `/api/admin/users`, `/api/admin/ads`, `/api/admin/settings` had zero auth enforcement — Every `/api/admin/*` handler must call `getAuthUser(request)` AND check `user.role === 'admin'`.
2026-06-05 — Logger emails in public API response (BUG-007) — `assignedLoggers` in the public `/api/matches` GET response included real email addresses — Never select or forward internal fields (email, loggerId, profileId) in public-facing responses.
2026-06-05 — Unauthenticated match/event creation (BUG-009, BUG-010) — `POST /api/matches` and `POST /api/events` had no auth gate — All mutation endpoints must auth-gate before reading the body.
2026-06-05 — Event type casing mismatch (BUG-012) — Rating calculator used `'GOAL'`, `'SAVE'` etc. but FootballLogger dispatches `'Goal'`, `'Save'` — Always normalize event type strings before comparison: `s.toLowerCase().replace(/[\s_-]+/g, '')`.
2026-06-05 — Hardcoded audit field (BUG-004) — `createdBy: 'admin-1'` hardcoded in transfers page — Audit fields must always source from the verified server-side session, never from a client-side constant or request body.
2026-06-05 — Race condition in logger assignment (BUG-008) — Non-atomic check-then-insert allowed duplicate assignments under concurrent requests — Wrap check + insert in a single Drizzle transaction; re-check inside the transaction.
2026-06-25 — Match stats all zero on Stats tab — API returns `shots: [home, away]` array format; LiveStats.tsx was reading flat `stats.homeShots` keys (undefined → 0). Also persisted `stats = '{}'` (truthy) blocked the computed-from-events path. Fix: dual-format reader in LiveStats + `statsEmpty` guard in GET route. Never read stat arrays as flat keys; always check the actual DTO shape from the API.
2026-06-30 — ~~Socket.IO `reconnect_failed` is never emitted in this codebase~~ — RETRACTED. Was only true because `sharedSocket.disconnect()` was called at `connect_error` attempt 5, which permanently killed Socket.IO internal reconnect state before `reconnect_failed` could fire. Session 38D removed that `disconnect()` call. `reconnect_failed` now fires correctly after 5 failed attempts. The `reconnect_failed` listener added in `74ca73b` is live and active.
2026-06-29 — Dedup keys with `Date.now()` always fail — `EventDrivenNotifier` used `${matchId}_${event.id}_${Date.now()}` as the dedup key, making every key unique. `sentNotifications.has(key)` never matched. Never include wall-clock timestamps in dedup keys.
2026-06-29 — Switch-case mismatch on multi-word event types — `type.toUpperCase()` on `'Yellow Card'` produces `'YELLOW CARD'` (space); switch case label is `'YELLOW_CARD'` (underscore). Always normalize `type.toUpperCase().replace(/\s+/g, '_')` before a switch on event type strings.
2026-06-29 — HAR confirms ~100 PATCH calls returned 200 — prior note "PATCH /loggers returns 404" was a false finding from a timeout window. Two 404s during Railway restart were the only failures. Never file a route as missing from log inspection alone; confirm with HAR ground truth first.
2026-06-25 — Period not persisting on Start 2nd Half / ET / Penalty buttons (TD-010) — inline `onClick` called only `transitionStatus()` (local state); no DB PATCH fired. `handlePeriodEndConfirm` had the PATCH but was only wired to End-of-Half buttons, not Start buttons. Fix: added fire-and-forget `fetch PATCH` to all three start-period buttons. Pattern: every period transition must write to both local state AND DB.
2026-06-25 — Friendly match events writing to player career stats — no `matchType` check before `updatePlayerStats()` call. Goals, cards, assists from friendlies inflated competitive stats. Fix: `if (match.matchType !== 'friendly')` guard. Applies to every stat-writing pipeline — always check match type before persisting.
2026-06-05 — Stored XSS via dangerouslySetInnerHTML (BUG-006) — `formatNewsContent` injected raw user text into HTML template strings without escaping — Always `escapeHtml()` user input before any template string injection; validate URL schemes before using in `href`.
2026-06-04 — Cannot find name 'initialData' TS Error — typeof self-referencing property in the interface definition — Define the default object above the interface and use typeof defaultObject.
2026-05-04 — Match Creation 500 Error — competitionId was sent as "" which SQL treats as a non-null value that fails FK constraints — Always coerce optional FK strings to null.
2026-05-04 — /api/events Unauthorized Access — Missing auth check in POST handler — Always call getAuthUser(request) in mutation handlers.
2026-05-04 — Unbounded List Queries — All list endpoints lacked limits, risking performance collapse — Always use .limit(50) on list queries.

2026-06-19 — Logger auth cookie never set (BUG-044) — `POST /api/loggers/auth` returned the JWT in the JSON body only. `getAuthUser()` reads from `authToken` cookie first — no cookie = 401 on every subsequent request, including PATCH match status and POST events. Root cause: treating the JSON response token as sufficient when the server-side auth layer reads from cookies. Fix: always call `response.cookies.set('authToken', token, ...)` before returning from any auth endpoint that issues a token for server-side consumption. Also store in `localStorage` if offline queue needs it — both paths need the token, in different forms.

2026-06-22 — getAuthUser() silently fails for logger sessions (BUG-057) — Logger JWTs carry `{ id, email, role }` but verifyAuth casts to AuthUser which expects `{ userId }` — so authData.userId is undefined. getAuthUser then queries the users table with undefined, finds nothing, returns null → 401 on every logger request despite valid authToken cookie. Fix: normalize `decoded.userId ?? decoded.id` in verifyAuth; branch on role==='logger' in getAuthUser to query loggers table. Pattern: any time two different JWT payloads (admin vs logger) share the same verify path, the downstream DB lookup must branch on role, not assume a single table. Never test auth by checking if login succeeds — test it by checking if a subsequent authenticated request succeeds.

2026-06-19 — Score update condition incomplete for new event types (BUG-047) — Score update used `type.toUpperCase() === 'GOAL' || value`. Client sends `'Penalty'` and `'Own Goal'` — neither matched. `value` is never in the payload. Every Penalty/OG event silently saved to DB without incrementing either score. Additionally, Own Goal reversed: `teamId` is the conceding team, so crediting `isHomeTeam = teamId === homeTeamId` gave the point to the wrong side. Pattern: whenever a new scoring event type is added to the client, the server-side score update condition must be explicitly expanded. Always list all scoring type strings; never assume the existing condition covers new variants.

2026-06-26 — /api/loggers/me existed but was completely inert (BUG-044b) — The endpoint read `x-logger-id` from a request header or query param with zero JWT auth — a pattern left over from the early prototype phase before `getAuthUser` was extended to support logger sessions. Any caller passing the right header could read any logger's profile. Fix: full rewrite using `getAuthUser` + role check. Pattern: whenever `getAuthUser` is extended to support a new role (admin → logger), audit all existing endpoints for that role and replace any legacy auth patterns (header params, query params) that predated the extension.

2026-06-26 — Single picker function serving both sub-OUT and sub-IN (BUG-067) — `getActiveRoster` had no concept of "on pitch" vs "available bench". Bench players who came on mid-match appeared in the sub-OUT picker (who's going off). Data integrity failure: a player could be logged as going off who was never actually on the pitch as a starter. Fix: two explicit functions — `getOnPitchPlayers` (starters − subbed off + subbed on) and `getAvailableBench` (bench − already subbed on − playerComingOut). Each picker must use only its correct pool. Never use a single roster function for pickers with different eligibility semantics.

2026-06-26 — DELETE /events score revert missing PENALTY + OWN GOAL, wrong OWN GOAL direction (BUG-054) — Parent DELETE route condition was `event.value || type === 'GOAL'`. PENALTY and OWN GOAL events were deleted from DB but score was never reverted. OWN GOAL additionally used `isHomeTeam = teamId === homeTeamId` — credits the conceding team, not the opponent. Pattern: whenever new scoring event types are added to the POST path, the DELETE revert path must be updated in the same commit. Always mirror POST scoring logic symmetrically in DELETE. OWN GOAL direction rule: `teamId` = conceding team → invert with `teamId !== homeTeamId`.

2026-06-26 — DELETE /events never decremented playerStats (BUG-060) — Event undo reverted the match score but left `footballPlayerStats`/`basketballPlayerStats` permanently incremented. Ghost goals/assists accumulated indefinitely. Fix: `decrementPlayerStats()` function mirrors `updatePlayerStats` with `Math.max(0, x - 1)` floor. Called after every event delete with same guards (skip friendlies, skip penalty shootout). Pattern: any route that increments stats on write must decrement on delete — these must be implemented together, never left asymmetric.

2026-07-01 — Railway WS CORS allowlist never included staging or Vercel preview URLs — `server.js` hardcoded `brixsports.com` and `www.brixsports.com` only. Every WS connection from `staging.brixsports.com` or a Vercel preview URL was blocked with a CORS error, making real-time untestable on staging. Fix: added explicit `staging.brixsports.com` entry + `origin?.endsWith('.vercel.app')` wildcard check. Pattern: whenever a new deploy environment is added (staging, preview), the Railway WS CORS allowlist must be updated in the same operation — Vercel and Railway are separate services and don't share env vars automatically.

2026-07-01 — `eye_point_awards` separate table duplicates `matchEvents` — `schema-enhanced.ts` introduced an `eye_point_awards` table storing matchId, playerId, awardedBy, reason, timestamp. All of these already exist on the `matchEvents` row (matchId, playerId, loggerId, detail, createdAt, isEyePoint). Two sources of truth for the same fact. Pattern: before creating a new table for an entity, check whether an `isX: boolean` flag on an existing table plus a filtered query covers the use case — it almost always does.

2026-06-26 — `|| value` in isScoringEvent triggered score increment on any event with a truthy value field (BUG-055) — `isScoringEvent = GOAL || PENALTY || OWN GOAL || value`. Jersey numbers, structured payloads, future event metadata passed as `value` would silently add 1 to the score. Fix: remove `|| value`, type-explicit scoring only. Pattern: scoring conditions must enumerate exact type strings. Never use a catch-all truthy check on a field that carries non-scoring data for other event types.

2026-06-26 — manifest-admin.json scope blocked iOS PWA install (BUG-075) — `scope: "/admin/"` excluded `start_url: "/admin?source=pwa"` (query string makes it non-child of scope) and the Logger shortcut at `/logger`. Browser silently ignored the scope, blocked install. Fix: `scope: "/"` for any PWA that covers multiple top-level routes. Rule: start_url must be a child path of scope — if the PWA spans multiple top-level routes, scope must be `/`.

2026-06-25 — playerComingOut timing gap in sub-IN picker — `matchState.events` does not yet include the in-progress substitution event when the sub-IN picker opens. Outgoing player was still visible. Fix: include `playerComingOut` directly in the exclusion set inside `getOnPitchPlayers`/`getAvailableBench` — it is always current React state, set synchronously before the modal opens.

2026-06-25 — `[eventId]/route.ts` DELETE + PATCH have zero auth (BUG-071) — Must gate with `getAuthUser` + role check (logger OR admin) before any UI calls these endpoints. Filed as CRITICAL.

2026-06-25 — Service worker IDB API mismatch (BACKLOG-058 drain crash) — `openDB()` returns a raw `IDBDatabase`. Calling `db.getAll(storeName)` or `db.delete(storeName, key)` on it throws `db.getAll is not a function` — these are Dexie.js patterns, not native IDB. In a service worker context, Dexie is not available. Raw IDB requires: `db.transaction(storeName, mode).objectStore(storeName).getAll()` wrapped in a Promise. Never use Dexie-style chained methods on a raw IDBDatabase object returned from `indexedDB.open()`.

2026-06-24 — Multi-affiliation player dropped from team roster (BUG-061) — `getPrimaryTeam()` resolves to the first `isPrimary: true` membership. Players with a college affiliation marked primary (and a separate BUSA team affiliation) resolve to their college team, not their match team. Filter `getPlayerTeam(p)?.id === teamId` then fails silently — player absent from picker with no error. Fix: always check `player.memberships?.some(m => m.team?.id === targetTeamId)` first before falling back to `getPrimaryTeam`. Never filter multi-affiliated players by primary team alone when the target is a specific match team.

2026-06-25 — Public viewer period label requires two data sources, not one (BUG-063) — `match.status` DB column stays `LIVE` throughout a match — it never holds `FIRST_HALF`/`HALF_TIME` etc. Period label comes from the WS `match:time:update` payload (field: `period`), which the logger emits from `FootballLogger`'s `useEffect` watching `matchState`. On initial page load or when no logger is connected, `matchTime` is `null` — WS period unavailable. The DB fallback is `current_period` (TD-010 column). Pattern: for any real-time field that has both a live (WS) and a persisted (DB) form, always derive display state as `liveSource ?? dbFallback`, never either alone. WS-only = broken on page load. DB-only = stale during play.

2026-06-25 — MatchStateManager broadcasts are DOM-only; socket emits are in FootballLogger — MatchStateManager's `broadcastTimeUpdate()`, `broadcastStatusChange()` etc. dispatch `window.CustomEvent` — local only, no socket involvement. The actual `emit('match:time:update', ...)` is in a `FootballLogger` `useEffect` watching `matchState`. Public page receives updates via socket, not from the manager directly. Don't assume adding a broadcast inside the manager will reach public viewers — the WS emit path lives in the component.

2026-06-24 — DB column added to schema but not returned by all relevant API routes (TD-010) — Added `current_period` column to `matches` schema and confirmed PATCH writes it correctly. However the `match` prop reaching `FootballLogger` comes from `GET /api/loggers/[id]` (the assigned matches endpoint), not `GET /api/matches/[id]`. If that route selects specific columns and omits `current_period`, the prop arrives as `undefined` on mount regardless of the DB value. Pattern: whenever a column is added to a schema, audit ALL routes that return that table's rows — not just the primary CRUD route. A column can be written correctly and still be invisible to consumers if a secondary read route omits it.

2026-06-19 — RESOLVED label applied without working end-to-end test (BACKLOG-058) — BACKLOG-058 (offline queue) was marked RESOLVED on code review. But logger auth was broken at the same time — `localStorage('authToken')` was never set, so queue-write path gated at `if (!token)` and returned early. No actual offline→queue→drain→sync cycle could have completed. The label propagated as a false positive. Rule: a feature is only RESOLVED when a successful live test cycle (not just code review) has been completed with all dependencies working.

2026-06-08 — Unauthenticated bulk player registration (BUG-013) — `POST /api/players/bulk-register` had no `getAuthUser` check — any unauthenticated caller could create player rows and teams — Always add `getAuthUser` + role check at the very top of every mutation handler, before reading the body.
2026-06-08 — Admin match cards showing raw team IDs (BUG-014) — Page fetched `/api/teams` with `.limit(200)` but 236 teams exist; teams beyond index 200 were missing, causing the `teamId → name` lookup to fall back to the raw ID string — The `/api/matches` response already embeds `homeTeam`/`awayTeam` objects — always use embedded response data before doing a secondary client-side lookup against a separately-fetched list.
2026-06-08 — Middleware login URL inconsistency — staging gate redirected to `/sign-in`, admin gate redirected to `/login`; these are different URLs, one produces a 404 — When writing auth redirects in different blocks of the same file, always use a single named constant for the login URL, not repeated string literals.
2026-06-08 — bash heredoc indentation breaks markdown in GitHub PR comments — heredoc body lines indented to match script indent level produce literal leading whitespace, breaking markdown tables — Always start heredoc content at column 0 when the content will be rendered as markdown.
2026-06-08 — Next.js middleware `pathname` never contains query string — `pathname.startsWith('/sign-in?')` never matches because `pathname` strips query params — Use `request.nextUrl.search` or `request.nextUrl.searchParams` for query string checks in middleware; `pathname` is path only.

2026-06-08 — PATCH /api/matches/[id] has no auth gate (BUG-015) — Any caller can update match scores, status, and internal audit fields with no authentication — Add getAuthUser + admin/logger check before body read in every PATCH/DELETE handler, not just POST. RESOLVED 2026-06-08.

2026-06-08 — POST /api/competitions has no auth gate (BUG-016) — Any unauthenticated user can create competitions — Every POST mutation in /api/* must have getAuthUser at the top, before reading the body. RESOLVED 2026-06-08.

2026-06-08 — Debug/test routes live in production (BUG-017) — /api/notifications/debug, /api/notifications/test, /api/email/test have no auth and expose internal config + subscription data — Never ship debug routes without auth. Delete or gate before any deploy. Pattern repeats BUG-003. RESOLVED 2026-06-08 — all three files deleted.

2026-06-08 — GET /api/matches/[id] leaks internal fields (BUG-018) — approvalStatus, managerNotes, loggerId returned in public response — Always shape a DTO for public responses; never spread the raw DB row. RESOLVED 2026-06-08 — explicit destructure excludes all banned fields.

2026-06-08 — Admin-purpose APIs callable without auth from any origin (BUG-019) — /api/admin/infrastructure and /api/analytics/system rely on middleware only — Middleware protects browser navigation; direct HTTP calls bypass it. Always add handler-level auth. RESOLVED 2026-06-08.

2026-06-08 — /live page has no real-time update mechanism (BUG-020) — Fetch on mount only; viewer must manually refresh to see score changes — Critical Flow C: public livescore must auto-update. Add polling interval or WS subscription. RESOLVED 2026-06-08 — polling interval changed 30s → 15s (stopgap; WS subscription still needed).

2026-06-08 — Hardcoded JWT fallback secret in auth.ts — Both verifyAuth() and generateToken() had process.env.JWT_SECRET || 'your-secret-key-change-in-production'. If JWT_SECRET is absent from env, all tokens are signed and verified with a publicly known string — any attacker can forge admin tokens — Remove the fallback entirely; import from env.ts which validates at startup. Never use || with a fallback on a secret value.

2026-06-08 — logger email leak via spread in match-logger-helpers.ts — ...a.logger! spread included email field of every assigned logger in responses accessible to the logger role — Never spread a DB row that contains PII (email, password, profileId) into an API response. Always use an explicit field shape. The password: undefined trick does NOT reliably remove a field — set it to undefined still appears on the object in some serializers.

2026-06-08 — Second handler in same file missed during auth sweep — analytics/loggers/route.ts had GET fixed but POST (leaderboard endpoint) was a separate export and was not caught in the sweep. Any caller could enumerate all logger emails via POST — When fixing auth on a file, always scan ALL exported function handlers, not just the first one visible. Two-handler files are common in Next.js route files.

2026-06-08 — Inline secret in node -e eval command — Turso auth token hardcoded directly in a node -e eval to run a DB query (turso CLI unavailable). Violates security rules — Never inline credentials in CLI commands or scripts. Always: import 'dotenv/config' + process.env. See .agents/rules/security.md.

2026-06-08 — Round label not rendering after first BACKLOG-032 commit — `/live` page only renders `status === 'LIVE'` matches; BUSALYMPICS/BUSA League matches are FINISHED/UPCOMING so they never appear there. Fix targeted the wrong page. Real render paths were `src/app/page.tsx` (homepage) and shared components (`MatchCard.tsx`, `MatchComponents.tsx`, `UpcomingMatchView.tsx`) — Always grep `match.competition` across the entire codebase before assuming a single page fix is complete.

2026-06-08 — round field not passed through page.tsx transform maps — `src/app/page.tsx` fetches from `/api/basketball/matches` and `/api/football/matches` (not `/api/matches`) and explicitly reconstructs match objects in 4 separate transform maps. `round` was not in any of them so it was silently dropped before reaching the component — When a page manually constructs match objects from API data, every field used downstream must be explicitly included. A spread (`...match`) would have avoided this; explicit maps are error-prone under field additions.

2026-06-08 — FK constraint failure on data migration with placeholder IDs — Directive specified competition IDs as human-readable slugs (`busa-league-football-2025`) that did not exist in the DB. Script applied cleanly in dry-run but failed at first UPDATE with `SQLITE_CONSTRAINT: FOREIGN KEY constraint failed` — Always verify FK target IDs exist in the DB before writing any migration script. Run a `SELECT id FROM [table] WHERE name LIKE '%...'` diagnostic first, not after the apply fails.

2026-06-08 — `.env.local` DB target assumed to be prod but was staging — No error, no warning; scripts ran against the wrong DB and produced the wrong audit output. Root cause: `.env.local` content changed between sessions with no visible indicator — Always run `identify-db.ts` (or print `TURSO_CONNECTION_URL` hostname) at the top of any script that writes to the DB. Never assume `.env.local` target from session memory.

2026-06-08 — Pre-prod check run against prod app instead of staging — Reported 10 failures that were expected (unfixed prod code). The check must run against the staging app (dev branch code) not prod. Root cause: `NEXT_PUBLIC_APP_URL` in `.env.production` pointed at `brixsports.com` — The clearance check validates that the dev branch's fixes work. Always run it against `brixsports-staging.vercel.app` using `.env.local`.

2026-06-11 — Backscoping via hard-delete instead of comment-out — First pass of BACKLOG-028 removed nav links and UI blocks with empty-string replacements (deletes). Convention is comment-out with BACKSCOPED marker. Root cause: convention was established mid-session after edits were already made. Fix: caught in git diff review and restored all deleted content as commented blocks before commit. Prevention: confirm preservation convention before first backscope edit, not after.

2026-06-11 — Rules of Hooks violation — `useEffect` placed after conditional `return` statements in `src/app/matches/[id]/page.tsx` — React requires hook call order to be identical on every render. If a hook appears after a `if (loading) return` guard, the hook is skipped on early renders, causing hook count mismatch between renders. Prevention: all hooks and derived consts they depend on must be declared above the first conditional return, using safe nullable access (`matchData?.match?.status`) rather than assuming the data is available.

2026-06-11 — Duplicate const declaration after hook move — Moving a `const` that appears after early returns to above them, without removing the original declaration, causes TS2451 (block-scoped variable redeclared). Same function scope, two declarations. Prevention: when moving a const up, always remove the original in the same edit pass. Run tsc immediately after.

2026-06-11 — Cherry-pick conflict when hotfix branch has features reinstated that dev has backscoped — `git cherry-pick` of a dev commit failed with 3-way conflict because the hotfix branch had Predictions/Polls imports and tab types active while dev had them commented out. The diff contained `setActiveTab('predictions')` on one side and `setActiveTab('overview')` on the other. Prevention: if a hotfix branch diverges from dev by having features reinstated, cherry-pick will always conflict on those files. Apply the fix manually instead.

2026-06-14 — College team logos empty string in DB rendered as broken img — `logo: ''` for COLNAS/COLENG/COLMANS/COLENVS. Direct `<img src="">` renders broken image. Prevention: always pass logo through `isValidLogo()` before rendering; use `TeamLogo` component everywhere, not raw `<img>`.

2026-06-14 — isValidLogo initially too strict, blocked working local paths — First implementation rejected all non-http paths, blocking `/assests/...` (local public folder) used by BUSA teams. Root cause: tried to enforce Cloudinary as part of validation. Fix: `isValidLogo` only rejects clearly broken values (empty string, null, undefined). Prevention: validation guards should protect against broken states, not enforce migration progress.

2026-06-14 — db:push fails when schema has an index that doesn't exist in live DB — `organizations_slug_unique` added to schema.ts after DBs were created, no push was run at the time. Drizzle-kit tries to delete the index on reconcile, fails. Prevention: after adding any index to schema.ts, immediately apply via SQL direct or db:push to staging. Never let schema and live DB drift.

2026-06-14 — staging and prod scores diverged silently for the same match — MD2 G1 had different homeScore on staging vs prod. No error surfaced. Prevention: after any PATCH script, always run a SELECT on both DBs and compare. Include a parity check at the end of every patch script.

2026-06-15 — Teams beyond .limit(200) silently missing from admin list — 236 teams in DB, cap was 200; the last 36 (including all 4 college teams) never appeared. No error, no warning — list just stopped. Root cause: limit set conservatively when DB had ~100 teams and never revisited. Prevention: when raising a limit, check actual row count first. Document the ceiling and set a backlog item when count approaches it (BACKLOG-045).

2026-06-15 — College team shortNames are CNAS/CENG not COLNAS/COLENG — searching "colnas" on the teams page finds nothing; searching "college" finds all 4 via the `name` field ("College of Engineering" etc.). The shortName abbreviation scheme is different from the old BUSA-style codes. Prevention: when investigating "missing" teams, query the DB to see exact shortName values before assuming a search bug.

2026-06-15 — Conditional field return based on role requires non-throwing getAuthUser — `getAuthUser` throws on invalid/missing token. Wrapping with `.catch(() => null)` in GET handlers that should be public but conditionally richer for admins. Prevention: when a public GET needs to return extra data for admins, always use `.catch(() => null)` pattern — never let an auth failure block the public response.

2026-06-15 — CRLF line endings break Edit tool string matching — 3 files (`search/page.tsx`, `livestreams/page.tsx`, `profile/page.tsx`) authored on Windows have `\r\n`. Edit tool `old_string` uses `\n` only, so match fails silently. Fix: node.js inline replace with `.replace(/\r\n/g, '\n')` before writing. Prevention: when Edit fails on a file that looks correct, suspect CRLF; open with a diagnostic node script to check.

2026-06-15 — Framer Motion `initial` prop causes hydration mismatch #418 — `initial={{ opacity: 0 }}` writes a `style` attribute on first client render not present in SSR HTML. `<motion.tr>` is especially bad: server renders `<tr>`, client renders a non-standard motion element tag. Prevention: never use `initial` prop on server-rendered components. Use `animate` and `transition` only; let elements start at their animated state.

2026-06-15 — Client-side tab filter with a non-null default hides data that has null in the filtered field — `/competitions` page defaulted to `sport: 'Football'`; competitions with `sport: null` were permanently invisible because no filter branch returned them. Prevention: any filter on a nullable column must have an explicit "All" / pass-through state as the default.

2026-06-15 — Directive field names diverge from actual type definitions — `RosterPlayer` uses `playerId` not `id`, and `nicknames` is already `string[]` from the GET handler — directive specified `match.player.id` and `JSON.parse(p.nicknames)`. Prevention: always read the actual type definition and GET handler before implementing anything that accesses a type's fields.

2026-06-15 — `dotenv/config` does not load `.env.local` — `import 'dotenv/config'` loads `.env` only. `.env.local` is a Next.js convention, not a dotenv default. Use `config({ path: '.env.local' })` explicitly in scripts targeting the staging DB. Prevention: template all dev scripts with the explicit path import.

2026-06-15 — Spec said `status !== 'archived'` but competitions schema uses `isArchived` boolean — filtering by `status === 'archived'` would always return false since that's not how archiving is modeled. Always read the schema before implementing a filter. The column name in the spec was an abstraction, not the actual DB column name.

2026-06-16 — `import 'dotenv/config'` does not load `.env.local` — loads `.env` only; `.env.local` is a Next.js convention, not a dotenv default. `process.env.*` returns `undefined` silently, causing a URL_INVALID crash at the libsql client. Always use `import { config } from 'dotenv'; config({ path: '.env.local' })` in dev scripts. `.env.local` = staging, `.env.production` = prod.

2026-06-16 — Dropped column name was snake_case in DB but camelCase in schema.ts — `match_duration` in the live DB, `matchDuration` in schema.ts. `ALTER TABLE DROP COLUMN matchDuration` returned SQL_INPUT_ERROR (no such column). Always run `PRAGMA table_info('table_name')` first to confirm exact column names before any DROP or ALTER.

2026-06-16 — SW serves stale HTML with old JS chunk hashes after deploy (BUG-026) — dynamic cache stored HTML pages indefinitely; after deploy, old HTML references chunk filenames that no longer exist on CDN → blank page or broken app on direct URL visit. Fix: HTML documents must never be cached by SW. Add `if (request.destination === 'document') { event.respondWith(fetch(request)...); return; }` at top of every SW fetch handler. Also add `Cache-Control: no-store` to SW files themselves so CDN never caches the SW.

2026-06-16 — SW path guard in the wrong layer caused a registration race — `PWAProvider` useEffect tried to suppress `usePWA`'s registration after the fact, but `usePWA`'s useEffect already ran first. Guard must live inside the hook (before `registerServiceWorker()` is called), not in the consumer. Prevention: suppression logic must always be co-located with the action it suppresses.

2026-06-16 — SW double-registration from two independent callers — `push-service.ts` called `navigator.serviceWorker.register('/sw-user.js')` independently while `PWAProvider` already registered via `usePWA`. Two `register()` calls for the same SW URL is benign (browser deduplicates) but creates scope confusion and bypasses any path guards in `usePWA`. Prevention: only one place should ever call `register()` for a given SW. Secondary consumers must use `getRegistration()` to receive the already-registered SW.

2026-06-16 — Free-text inputs for canonical enum data caused DB casing drift — college codes (`ColEng`, `Colmans`, `''`) entered by admins via a plain `<input type="text">` instead of a select. Required a one-off cleanup script to fix 6 rows. Prevention: any field with a fixed set of valid values must use `<select>` in every UI that writes it. Never trust admins to type canonical codes correctly.

2026-06-16 — `useLiveStandings` emoji fallback (`|| '❓'`) masked null team logos — hook returned a literal emoji string instead of null, so `TeamLogo` received an emoji and tried to render it as a URL. `isValidLogo` rejected it, triggering initials — correct output by accident, but wrong data type. Prevention: when a child component handles null gracefully, always pass null — not an emoji or placeholder string — as the null-state value.

2026-06-16 — match_events rows with player_id = NULL corrupt playerStats silently (BUG-032) — 39 events exist with no player reference. They count toward match scores but cannot be attributed to any player. The logger UI and backfill import both allow saving an event with no player selected — this must be blocked at the API level. Prevention: POST /api/events must validate that player_id is present for all event types that affect player stats (goals, cards, assists, substitutions). Backfill import must reject CSV rows where the player column is unmatched rather than inserting a NULL player_id row.

2026-06-16 — users.favorite_team_id silently blocks team deletion — FK constraint failed when deleting stub teams because two user accounts had them set as favourite_team_id. This column was not in the initial FK scan because the table was named `users` not `user_profiles`. Prevention: before any team delete, always query users.favorite_team_id in the pre-flight check alongside affiliations and matches. The PRAGMA foreign_key_list scan is the reliable fallback when you don't know all child tables.

## Anti-Patterns for This Codebase
- Middleware matcher and internal logic check do not match.
- try/catch without finally in any DB operation.
- List query with no .limit() clause.
- createdBy, updatedBy, or any audit field set to a hardcoded string.
- Auth check that only runs client-side with no server-side counterpart.
- Business logic that assumes valid token = valid role.
- TODO or FIXME comment touching auth, permissions, or data access.
- Commented-out security checks with `// temp` or `// disable for testing`.
- UI shows success state before server response is confirmed.

## Constraints Learned From Failures
- Viewers never have a session. Never assume otherwise.
- A valid JWT does NOT equal valid permissions. Always verify role explicitly.
- Admin API routes must call `getAuthUser(request)` AND check `user.role === 'admin'` — never trust middleware alone.
- Live update mechanism must have a fallback if the channel drops.
- Viewer must see stale data clearly on failure, not a crash.
- Target update latency: under 5 seconds from event save to public display.

2026-06-17 — Empty string teamId causes FOREIGN KEY constraint failure on player create — `body.teamId` arrives as `''` when no team selected; DB rejects it as a non-null FK reference. Fix: always coerce `body.teamId || null` before insert. Applies to any optional FK column.

2026-06-17 — players.number is NOT NULL in DB despite nullable intent — schema has `notNull().default(0)` so null is rejected at DB level. Used `body.number ?? 0` as workaround (0 = unassigned sentinel). Real fix: make column nullable via migration (BACKLOG-072).

2026-06-17 — mirror script (UPDATE) silently no-ops when target rows don't exist on prod — `rowsAffected: 0` for all 30 new players because UPDATE finds no matching IDs. New player rows require INSERT not UPDATE. Always check whether target rows exist on prod before writing a mirror script; if new profiles may be absent, build an INSERT-with-skip-existing script instead.

2026-06-17 — UI form required attribute blocks valid partial profiles — Jersey # and Assigned Team marked `required` in HTML prevented BUSALYMPICS-only players (no team, no jersey) from being created, even though the API and DB allow it. Browser enforces HTML required before the request is sent. Prevention: only add `required` to inputs that the server also enforces as required.

2026-06-17 — Backfill script without sport guard (BUG-033) — college affiliation backfill inserted all players with matching college field into football college team IDs, regardless of sport — always join through the player primary team sport before any affiliation INSERT; add AND (primary_team.sport = 'Football' OR no primary team) guard to every college backfill script.

2026-06-17 — Per-match event route missing auth (BUG-034) — /api/matches/[id]/events POST had no getAuthUser() while /api/events (the other event route) was correctly gated. Two routes serving the same function, only one audited — when adding auth to a route, grep for sibling routes at the same resource level (/api/events AND /api/matches/[id]/events) and audit all of them together.

2026-06-17 — loggerId from client body (BUG-034) — event creation accepted loggerId from the request body, allowing any caller to attribute events to any logger — audit fields (loggerId, createdBy, updatedBy) must always be sourced from the verified authUser, never from the request body.

2026-06-19 — Logger had zero service worker coverage (BACKLOG-093) — `usePWA.ts:13-16` blocks `sw-user.js` on `/logger` paths; `src/app/logger/layout.tsx` did not exist so no SW was ever registered for loggers. Result: no offline cache, no background sync, no push. Fix: `src/app/logger/layout.tsx` wraps children in `<PWAProvider swPath="/sw-admin.js" scope="/logger">`. Prevention: any new path added under `/logger` or `/admin` must have an explicit `layout.tsx` with `PWAProvider`. The root layout's SW is always blocked on these paths.

2026-06-19 — sw-admin.js syncMatchEvents() POSTed without auth to a gated endpoint — tokenless events hit BUG-034's now-gated endpoint, returned 401 indefinitely, triggered an infinite background sync retry storm. Root cause: SW background sync fires outside any browser session — no cookie available. Fix: store JWT at queue-write time in the IndexedDB row; skip (continue) any row with no token in syncMatchEvents(). Prevention: any background sync fetch to a JWT-protected endpoint must embed the token in the queued row at write time, not retrieve it at drain time.

2026-06-19 — Start Match and End Match transitioned local state before PATCH resolved (BUG-049) — `transitionStatus('FIRST_HALF')` and `transitionStatus('FINISHED')` both called before the PATCH, with PATCH errors only logged to console. Logger saw "live" / "finished" while DB held the old state. Fix: PATCH first, check `res.ok`, only then call `transitionStatus`. On failure: `alert()` + re-enable button. Prevention: any flow that writes server state AND transitions local UI state must do the server write first and gate the local transition on confirmed success. The pattern applies to every status change, not just Start/End.

2026-06-19 — PATCH /api/matches/[id] accepts any status string from any authenticated logger (BUG-051) — no enum validation on `body.status`, no role restriction. A logger can directly set `status: 'FINISHED'` without going through `handleFinalize`, or corrupt the status field with an arbitrary string. Fix: validate against the allowed enum; restrict loggers to `['LIVE']` only. Prevention: any freeform string field that maps to a constrained domain (status, role, sport) must be validated against an explicit allowlist in the handler — never trust the client to send valid enum values.

2026-06-19 — JWT_SECRET fallback to public string in loggers/auth (BUG-050) — `process.env.JWT_SECRET || 'your-secret-key-change-in-production'` — if env var is absent, all tokens are signed with a known public key. Fix: import from `env.ts`, no fallback. Prevention: never use `||` with a string fallback on any secret value. `env.ts` + `validateEnv()` at startup is the correct pattern; it fails fast instead of silently degrading to an insecure state.

2026-06-22 — AuthContext wipes localStorage.authToken for logger sessions → offline queue gets null token (BUG-058b) — `AuthContext.checkAuth` runs on every page mount. It calls `GET /api/auth/me`, which returns 401 for loggers (admin-only endpoint). On 401, line 74 calls `localStorage.removeItem('authToken')`. FootballLogger's offline catch block reads `localStorage.getItem('authToken')` — gets null — hits the `!token` guard — fires alert instead of queuing. Fix: (1) `POST /api/auth/refresh` now supports logger tokens and returns the new token in the response body; (2) FootballLogger mount effect calls refresh on load and re-writes the token to localStorage, guaranteeing it survives the AuthContext wipe. Prevention: when two components in the same session both touch `localStorage.authToken` — one reading (FootballLogger) and one deleting (AuthContext) — the read must come from a source that outlasts the delete. A cookie-backed refresh call is the durable source.

2026-06-24 — Timeline tab TypeError masked as network 500 (BUG-059) — `LiveMatchTimeline` received `eyePoints` as `undefined` (API never returns that key) and called `.length` unconditionally. Client-side render crash appeared as a failed network request in DevTools — not a server 500. Sentry captures this as a client exception, not a server error log. Prevention: any component prop typed as an array must be guarded with `?? []` if the API response shape does not guarantee the key. When a "500" appears in the network panel, verify it is actually a 5xx HTTP status before assuming server-side throw — render crashes can manifest as network failures on dependent requests.

2026-06-24 — RESOLVED label applied without DB evidence (BUG-047 false close) — BUG-047 was marked RESOLVED on "logger showed 2-3." Logger score is derived from locally-dispatched events, not from `matches.homeScore`/`awayScore`. These can differ. The rule is now codified in CLAUDE.md: for any bug that writes to the DB, evidence must include a DB query result showing actual stored values. UI state, HTTP 201 codes, and absence of console errors are explicitly invalid as sole evidence.

2026-06-24 — Period state is ephemeral — phone refresh during live match resets logger to NOT_STARTED at 0:00 (TD-010) — Period transitions (`handlePeriodEndConfirm`, "Start 2nd Half") only update React `useState`. No server PATCH. DB shows `status: LIVE` but logger reloads with no period context. Public page appears to see a match reset. Elevated to CRITICAL pre-live-match blocker. Fix: `currentPeriod` column on `matches`, PATCH on every transition, read on mount. Prevention: any match state that must survive a browser reload (period, half, status) must be persisted server-side. Local-only state is acceptable only for ephemeral UI (animation, scroll position).

2026-06-24 — footballPlayerStats not rolled back on event DELETE (BUG-060) — `DELETE /api/matches/[id]/events` correctly reverts `matches.homeScore`/`awayScore` but never calls `updatePlayerStats` in reverse. A deleted goal leaves the scorer with a ghost goal in their career stats permanently. Also: Penalty and Own Goal event types hit the score pipeline but not `updatePlayerStats` at all — no stat written for the player who scored them. These are architectural holes in the stats increment model; correct long-term fix is event-sourced recompute from `match_events` on demand.

2026-06-25 — [eventId] route had zero auth — any unauthenticated caller could delete any match event (BUG-071) — `DELETE` and `PATCH` on `/api/matches/[id]/events/[eventId]/route.ts` had no `getAuthUser` call at all. The parent route (`DELETE /api/matches/[id]/events`) had the full auth gate; the individual-event route was written separately and never received it. Additionally the DELETE only reverted GOAL score — PENALTY and OWN GOAL were not handled, and OWN GOAL inversion was wrong (reverted `teamId` team rather than opponent). Fix: mirror parent route's auth gate exactly; fix OWN GOAL inversion (`isHomeTeam = event.teamId !== match.homeTeamId`). Prevention: when a new route file is created for a sub-resource (`[eventId]`), always audit auth parity with the parent route before committing. A shared auth helper is not enough — each route file is independently responsible.

2026-06-25 — Undo button updated local state before DB confirmed (pre-fix pattern) — `handleUndo` called `manager.undoLastEvent()` synchronously, then emitted WS broadcast. No server DELETE was called at all. Score reverted locally, DB kept the event, public page diverged. Fix: make `handleUndo` async, call `DELETE /events/[eventId]` first, gate `undoLastEvent()` on `res.ok`. Pattern: any handler that mutates both server and client state must write server first, gate client update on confirmed success — same rule as BUG-049 (Start Match ghost state). This is the same class of bug.

2026-06-19 — Two parallel IndexedDB implementations, wrong one wired (BACKLOG-058) — `offline-queue.ts` writes to `brixsport-offline.events`; `sw-admin.js` drains `BrixsportAdminDB.pendingMatchEvents`. FootballLogger catch block was `console.error` only — neither DB was written to. Fix: FootballLogger writes directly to `BrixsportAdminDB.pendingMatchEvents`. Prevention: before wiring any IndexedDB write, grep for the DB name and store name to verify the drain side targets the same DB. Mismatched DB names = silent data loss with no error.

2026-06-25 — Match status permanently stuck on LIVE after normal match end (BUG-076) — `handleFinalize` (the only path writing `status: FINISHED`) was guarded by `currentPeriod !== 'FINISHED'`. Period transitions to FINISHED via `handlePeriodEndConfirm`, which re-renders and hides the End Match button before `handleFinalize` can be called. Every match with different scores ended permanently stuck on LIVE. Fix: when `nextPeriod === 'FINISHED' && homeScore !== awayScore`, fold `status: FINISHED` into the same period-transition PATCH. Never split "period reaches FINISHED" and "match status becomes FINISHED" into two separate user actions — they must be atomic for the decisive whistle. ET/Penalties paths are the only exception (period can be FINISHED while match continues to shootout).

2026-06-25 — HAR status=0 ≠ server never received the request — Page navigation during in-flight fetch cancels the response receipt on the client and records status=0 in HAR. The server may have already processed the request. In the KIN vs JOG test, 8 events showing status=0 all appeared in the DB (SW queue resent them after reload, server got 2 copies but idempotently handled them). Do not treat HAR status=0 as evidence of server non-receipt — always verify via DB query.

2026-06-25 — Background Sync API (sync.register) is not supported on iOS Safari — Any offline queue drain that relies solely on `ServiceWorkerRegistration.sync.register()` will silently no-op on all iOS devices. Events write to IndexedDB correctly but never drain automatically. Fix: always implement an `online`/`visibilitychange` page-context drain as the primary trigger; Background Sync is Android-only and should be a secondary fallback.

2026-06-26 — 4xx server rejection on event POST had zero UI feedback (BUG-056) — The `else` branch after `if (res.ok)` in FootballLogger's event POST handler only called `console.error`. A 401 mid-match (token expiry) silently dropped the event with no logger awareness. Fix: always surface 4xx rejections as visible alerts in any logger action path — distinguish 401 (re-login required) from 403 (assignment issue) from generic server error. Never leave a logger-facing error path with only a console.error.

2026-06-26 — Disabled button with no explanation is a UX anti-pattern for admin publish flows (BUG-043) — The Publish Lineups button was disabled when captains weren't set but showed no message. Admin users were confused when 11/11 starters were confirmed. Fix: always render an inline conditional message below disabled publish/submit buttons explaining the unmet condition. Do not rely on the disabled visual state alone.

2026-06-27 — React useState ignores prop updates after mount (BUG-079 Part 2) — Modal component initialises `matchSettings` via `useState(initialMatchSettings ?? defaultMatchSettings)`. Parent fetches settings async and updates the prop after the modal is already mounted — `useState` never re-runs, defaults persist. Fix: `useEffect` that calls `setMatchSettings(initialMatchSettings)` when the prop changes. Prevention: any component that receives async-loaded data as an initial prop must use `useEffect` to sync state, not rely on `useState` initial value alone. This applies to any modal/drawer that opens before data loads.

2026-06-27 — API returns array shape but UI reads it as object (BUG-079 Part 1) — `GET /api/competitions/[id]/match-settings` returns `{ settings: [...] }`. The call site destructured `data.settings` and accessed `.maxSubstitutions` directly — all fields were `undefined`, form showed hardcoded defaults on every open. DB write was always correct; only the read was broken. Prevention: when consuming a GET response, always verify the shape with a console.log or a runtime Array.isArray check before accessing properties. Never assume an API returns a plain object when it could return an array wrapper.

2026-06-27 — displayMinute clamped at regulation ceiling, not real stoppage minute (BACKLOG-110) — `updateDisplayMinute()` uses `Math.min(absoluteMinute, halfDuration)` — stoppage-time events (47') get stamped as `45'`. The clock face is correct (reads `absoluteMinute` via `getFormattedTime()`). The two sources diverged silently. Prevention: `displayMinute` and the clock face must read from the same source. Using separate derivations for "display" and "storage" of the same value is a recipe for silent divergence.

2026-06-29 — filterStartersOnly silently filtered subbed-on players from all non-sub event pickers (BACKLOG-106 real root cause) — `PlayerSelectionModal` had `filterStartersOnly=true` for every non-sub event type. The internal filter `!starterIds.has(p.id)` excluded any player not in the original lineup starters — subbed-on players are never starters, so they were always stripped even though `getOnPitchPlayers` correctly added them to the `players` prop. The picker received the right list and immediately filtered it down to the wrong one. Zero error, zero warning. Prevention: whenever a component receives a pre-filtered list as a prop AND applies its own filtering internally, trace both filter paths. Prop-level filtering and component-level filtering are independent — passing the right players in doesn't mean the right players come out.

2026-06-29 — PWA scope /logger vs /admin — logger layout referenced admin manifest (BUG-075) — `manifest-admin.json` has `start_url: "/admin?source=pwa"`. Logger layout (`src/app/logger/layout.tsx`) referenced this manifest. Installing the PWA from `/logger` on iOS silently installed an app that launched to `/admin` — a page loggers cannot access. Never share a manifest between two app sections that have different entry points. Each distinct user role with a different start URL must have its own manifest file.

2026-06-29 — `decrementPlayerStats` defined in collection route, unreachable from individual-event route (BACKLOG-111 root cause) — `decrementPlayerStats` was added to `events/route.ts` (the `/api/matches/[id]/events` collection route). The undo path calls `DELETE /api/matches/[id]/events/[eventId]` which routes to `[eventId]/route.ts` — a completely separate file. The function was never exported, never imported, never called. Stats were not reverted on any undo. Fix: self-contained `revertPlayerStat` function written directly in `[eventId]/route.ts`. Prevention: when adding a helper to a route file, immediately check whether sibling/child route files need the same capability. A helper function that is not exported is only reachable within that single file — if the feature touches multiple route files, extract to a shared lib or duplicate with intent.

2026-06-29 — Second yellow undo only removed the Red Card, not the triggering Yellow Card (BUG-072) — `handleUndo` read the last event and issued one DELETE. When a second yellow is logged, `MatchStateManager.recordEvent` auto-inserts a Red Card as the next event. Undo removed only that Red Card — the Yellow Card stayed in both DB and local state. Fix: detect `detail === 'Red Card (Second Yellow)'`, find preceding Yellow Card for same player, issue a second DELETE, call `undoLastEvent()` twice. Prevention: any auto-generated event that is inserted in response to another event must have a cascade undo path. If event A always triggers event B, then undoing B must also undo A.

2026-06-29 — Dead route handler silently coexisted with its replacement for 10+ sessions (BUG-054 parent route) — `DELETE /api/matches/[id]/events?eventId` was the original undo endpoint. When `[eventId]/route.ts` was built as its replacement, the old handler was never deleted. It had its own `decrementPlayerStats` helper (not exported, not reachable from any other file) that also silently became dead. Neither TypeScript nor runtime gave any signal — the route was simply unreachable from the UI. Prevention: when superseding a route with a new one, delete the old handler in the same commit. A live handler that nothing calls is a maintenance hazard — it accumulates drift from the real path and misleads future readers.

2026-06-29 — Same "no sub awareness" bug in two separate modals — BACKLOG-106 fixed `PlayerSelectionModal` (general event picker) but `PenaltySequenceModal` had an identical `starterIds`-only check. The root cause (on-pitch status computed from lineup starters only, ignoring subbed-on players) manifested in two independent components. Session 35 fixed one; session 37 found the other. Prevention: when fixing a modal prop/logic pattern, grep for all modals that render player lists and check each one — a pattern bug is usually present at every call site, not just the one that surfaced first.

2026-06-29 — try/catch dropped on stat reversion rewrite, creating false 500 after successful delete — `revertPlayerStat` was written as a clean replacement for `decrementPlayerStats`. The original function had an explicit `// Don't throw — event deletion already succeeded` guard. The rewrite dropped it. Result: any DB error during stat reversion caused the outer try/catch to return 500 — event was already deleted, logger got a false failure. Prevention: when rewriting a function that explicitly swallows errors, the new version must preserve that contract. Search for `// Don't throw` or `// ignore` comments before rewriting — they are intentional, not cleanup targets.

2026-06-30 — httpOnly cookies are invisible in HAR exports — not a missing-cookie bug — HAR specification intentionally omits httpOnly cookies from the `cookies` array. If a fetch returns 401 and HAR shows `cookies: []`, this does NOT mean the cookie wasn't sent — it means the cookie is httpOnly. Check the server-side `request.cookies.get(...)` log, not the HAR. Never file a "cookie not being sent" bug based solely on a HAR with empty cookies.

2026-06-30 — `getAuthUser` logger branch returns null for logger-role users registered via viewer path — `getAuthUser` branches on `role === 'logger'` and queries the `loggers` table. A user who registered via the viewer login path can have `role='logger'` in the `users` table. Their JWT carries their `users` table ID, which is not present in the `loggers` table. The branch returned null (401) instead of falling through to the `users` table. Fix: if loggers-table lookup misses, fall through to users-table lookup. Pattern: any time a role-based lookup can fail for an actor who has that role set in a different table, the lookup must have a fallback — never return null on a single-table miss if the role can exist in multiple tables.

2026-06-30 — Logger cookie bleed into viewer-app routes — `authToken` cookie is set with `path: '/'` (domain-wide). Logger JWTs carry the logger's `loggers` table ID. Viewer-app routes that query `userFavorites`/`userFollows` (keyed to `users.id`) receive a logger ID that matches no user row → empty results. Not a 401 — auth passes but query returns zero rows. Fix: `resolveEffectiveUserId()` bridges the gap by looking up the logger's fan account in `users` by email. Long-term fix is SSO (BACKLOG-117). Until then: every viewer-app route that queries user-scoped data must call `resolveEffectiveUserId()` before the DB query.

2026-07-01 — BUG-084 false filing: "pushSubscriptions always empty, no enrollment UI" — assumption made from an early diagnose endpoint result before any user had onboarded. DB query (2026-07-01) confirmed 3 real subscribers on prod. Enrollment UI exists in SettingsOverlay, OnboardingModal, and NotificationPermission — all three call `PushService.subscribe(userId)`. Never file a table-empty bug without a direct DB count. Never assume a feature is unwired without grepping all call sites of the implementing function.

2026-07-01 — Shared VAPID keys across staging and prod — staging test match delivered a real VAPID push to prod subscribers. Root cause: VAPID private key identical in both Vercel projects + shared Railway WS server emitting `io.emit('notification:global')` to all sockets. Fix: rotate VAPID keys per environment, generate separate JWT_SECRET per environment. Prevention: any key that controls push delivery, token signing, or DB access must be unique per environment. Shared Railway WS compounds the problem — `io.emit` is not room-scoped.

2026-06-29 — console.error in API route does not reach Sentry — assumed Sentry would capture any server-side error. It does not. `console.error` goes to Vercel function logs only. Sentry only captures what is explicitly passed to `Sentry.captureException()` or unhandled exceptions propagating through the global error handler. For swallowed errors (try/catch where you intentionally don't rethrow), always add `Sentry.captureException(error)` explicitly. Prevention: whenever writing a catch block that returns a success response, ask "will this error be visible somewhere?" — if the answer is "Vercel logs", add Sentry.

2026-07-09 — Recompute logic scoped to "the current batch" silently overwrites cross-batch history — a stats-recompute step that counts only the events being inserted RIGHT NOW (not a player's full history) is correct only as long as no player ever appears in two processed batches. The moment they do, the second batch's recompute overwrites the first batch's contribution instead of adding to it — no error, no constraint violation, just a quietly wrong number. This surfaced in the BUSALYMPICS backfill (MD1 g1 used scoped recompute; g2 needed cumulative) and in the process uncovered a second, independent instance of the same root problem already live in production (a one-time global stats zero had wiped a real match's cached stats, since the zero never touched the underlying event history). Prevention: any recompute/aggregate-rebuild step must always source from the full authoritative history (query "all of X's events", not "the events I'm inserting this run"), never from the current operation's local scope. Treat this as a hard rule for any script that derives a stored aggregate from an append-only log.

2026-07-09 — Matching/dedup logic checked for duplicate relationships but not conflicting ones — the player-matching pipeline correctly prevented creating a second identical (playerId, teamId) affiliation row, but had no check for whether a NEW affiliation would conflict with an EXISTING one in the same exclusive category. Club-team affiliation is legitimately multi-valued (a player can be on a club AND a college team at once) — but college affiliation is exclusive (one college per student). Nothing distinguished these categories, so a wrong platform-wide match silently gave one real player two simultaneous college affiliations, with no constraint violation to catch it. Prevention: when a matcher/dedup step creates a new relationship, check not just "does this exact relationship already exist" but "does a CONFLICTING relationship in the same exclusivity class already exist" — and know which categories are exclusive vs. multi-valued before writing the check. Fix shipped as `dev/lib/college-guard.mjs`, wired into both the matcher (flags before human sign-off) and write-script pre-flight (hard abort).

2026-07-09 — Nickname written to the wrong per-team affiliation row makes a player invisible to that team's own roster search (recurred 3x: Mayokun, Olusanya, Ayomiposi Alabi) — `player_team_affiliations.nicknames` is scoped per affiliation row, not global to the player. Writing a nickname meant to help future matching to the WRONG affiliation (e.g. a player's club team instead of their college team) means a team-scoped search for that alias finds zero candidates, falls through to a platform-wide search, and can surface a completely unrelated person (in one case, an inert duplicate player record; in another, a different-sport athlete). `players.jersey_name` is a single global value and is NOT the right fix for "known by a different name in a different team context" — renaming it overwrites the player's real identity everywhere. Prevention: when adding a nickname to help a specific team's matcher find a player, always write it to that exact team's `player_team_affiliations` row, verified by `team_id`, never assume `players.jersey_name` or a nickname on a different affiliation will be visible.

2026-07-09 — Cumulative stat recompute must re-derive from full event history every time, never "add a delta to whatever is currently stored" — a variant of the scoped-recompute bug above, caught in a draft before it ran: computing `shots_on_target` as `existing_stored_value + this_match_contribution` instead of `total('Shot on Target') + total('Goal') + total('Penalty')` from the complete match_events history. The delta-add form is fragile in the same way scoped recompute is — it silently compounds if a script is ever re-run, and it inherits any staleness already present in the stored value instead of self-correcting. Prevention: any script producing a stored aggregate must compute the WHOLE value from source events every time, never increment the previous stored number.

2026-07-11 — `competition_team_entries` UPDATE silently no-op'd because the table had zero rows for the target competition — attempted to seed `group_name` via `UPDATE ... WHERE competition_id = ? AND team_id = ?` for 16 BUSA League teams; every UPDATE returned `rowsAffected: 0`. The table only had 4 rows platform-wide (all BUSALYMPICS) — not just missing `group_name`, the rows themselves never existed. Caught before being mistaken for success by explicitly checking `rowsAffected`, then confirmed via `SELECT COUNT(*)`. Same shape as the established "mirror scripts must INSERT, not just UPDATE" pattern — reconfirmed here on a different table. Prevention: never assume a seed/backfill target table already has rows for the entity in question; check row existence explicitly before choosing UPDATE vs INSERT.

2026-07-11 — Player-matcher name-resolution fallback was too loose, matched cross-sport and cross-competition teams — extending `backfill-match-players.mjs`'s `resolveTeamId` with a bidirectional substring match against `teams.name` (to handle BUSA League's 3-letter `short_name` codes not matching a sheet's fuller tab name) initially had no scope restriction — "KINGS" matched "Vikings" and "Kings University" from unrelated competitions/sports. Fixed by scoping the fallback query to `id LIKE 'busa-%'` only. Separately, several individual player candidates (Jabbar, OLA, Ebuka, SALIM, PAUL) turned out to be real players on **basketball** teams (Storm, Titans, TBK, Rim Reapers) that the matcher's fuzzy/cross-team search surfaced as false-positive candidates for football sheet names. Prevention: any team-name fallback search must be scoped to the relevant competition/sport pool, not run platform-wide; always check a fuzzy candidate's actual sport before accepting it, not just its name similarity.

2026-07-11 — Ad-hoc duplicate-player detection scripts missed real duplicates that the already-proven matcher (`backfill-match-players.mjs`) would have caught — built several new one-off scripts to compile and verify "new player" candidates instead of using the established, self-tested matcher's own CREATE STUB recommendation. This reintroduced the same bug class repeatedly: missed a match stored only in `jersey_name` not `name` (BRICKS), missed a match where the sheet has only a first name but the DB has "First Last" (DAVID vs "David Disu"), and completely missed the `team_id IS NULL` unassigned-player pool (Tisco Jr and 12 others) until a dedicated pass was added for it. Root cause: rebuilding matching logic from scratch instead of extending a tool that already had 16/16 self-tests and had already correctly handled these exact shapes elsewhere in the same session. Fix going forward: added a `--json` export flag to the matcher itself so its recommendations are consumable programmatically by write scripts, rather than continuing to re-derive matching logic ad hoc. Prevention: before writing new matching/duplicate-detection logic, check whether an established, self-tested tool already solves the same class of problem — extend it, don't parallel it.

2026-07-11 — `matches.start_time` has no real "TBC"/placeholder mechanism anywhere in the stack — the column is `NOT NULL` with no default at the DB level (confirmed via `PRAGMA table_info`), the `POST /api/matches` API route has zero explicit validation (relies entirely on the DB constraint, would surface as a raw unhandled 500 if bypassed), and the admin form's `required` attribute on the datetime input is load-bearing, not decorative — the edit-save handler calls `new Date(formData.startTime).toISOString()`, which throws outright on an empty string. Prevention: never assume a "TBC" or placeholder value can be safely inserted for a NOT NULL column without checking (a) the DB constraint, (b) whether the API route validates independently, and (c) whether any downstream code path (not just the write path) assumes the value is always well-formed.

2026-07-11 (session 41B) — Duplicate-player-with-real-events is a distinct, more dangerous variant of the Lazzy Woods duplicate-player bug — three more cases found in the Hammers roster while building busa-match-13's events ("Sancho" #8, "Speedy" #11, "Spectrum" #10: a real, fully-named roster player exists at the same jersey number as an inert-looking stub). Unlike Lazzy Woods, two of these three duplicates were NOT inert — they carried real `match_events` + `football_player_stats` + `player_ratings` from the one live-logged match already in the DB (`8Mek2CA7KPlnk1EQ647jx`, 3rd Place Playoff). A naive "just delete the duplicate" fix would have silently destroyed real match history. Prevention: before deleting any suspected duplicate player, always run a full `PRAGMA foreign_key_list` scan for `match_events`/`football_player_stats`/`player_ratings` rows first — if any exist, the fix is a re-point-then-delete merge (UPDATE player_id on every referencing row, cumulative recompute, then delete), never a straight delete. Same shape as the Israel Emmanuel fix, just without a college-affiliation angle.

2026-07-11 (session 41B) — Nickname-scoped-to-wrong-affiliation recurred a 4th time on the SAME player across two different affiliations — Olaoluwa Olusanya's "Lazzy" nickname was correctly added to his COLENG affiliation in session 40C, but his separate busa-hammers CLUB affiliation still had `nicknames: "[]"`, so a busa-hammers-scoped search for "LAZZY" found nothing and recommended CREATE STUB. Confirms the established rule ([[project_nickname_scoping]] pattern) applies per-affiliation-row, not per-player, even when the player already got the fix once on a different affiliation. Prevention: when a player has multiple affiliations (club + college, or multiple clubs), a nickname fix on one affiliation does NOT cover the others — check every affiliation row that a team-scoped search could hit.

2026-07-11 (session 41B) — `players.number` can silently disagree with every source-of-truth logsheet — Timi (busa-hammers) was recorded in the DB as jersey `#5`, colliding with Iyanuloluwa Olusore's real `#5` and producing an ambiguous jersey lookup, while every logsheet (2 independent matches) consistently showed his jersey as `#18` (confirmed free in the DB). This is a plain data-entry error, not a duplicate-player or nickname-scoping issue — root-cause fixed by correcting `players.number` and `player_team_affiliations.jersey_number` directly after confirming `#18` was unoccupied. Prevention: when a jersey-number-based player lookup returns more candidates than logsheets ever show at that number, suspect a wrong `number` field before assuming ambiguity or a duplicate person — cross-check against every sheet the player appears in.

2026-07-11 (session 41B) — **BUSA League matches are 35-minute halves — full time is minute 70, not 90.** A logsheet's `minuteOut` column records when a player's involvement ENDED, which for a non-substituted player is simply full time (70), not a substitution. First BUSA League event-write (busa-match-13) wrongly treated every non-null `minuteOut` as a real substitution-out, producing 9 simultaneous "70th minute" substitutions for players who simply played the whole match, plus a literal duplicate row for one player, all with the wrong event shape besides (see the paired-substitution-convention entry above). Caught only because the pattern was implausible on inspection (a whole team doesn't sub 9 players in one minute) — fixed by deleting all 12 wrong rows and rebuilding only the two genuine early-exit pairs found by minute (`< 70`), one of which (AHIMA→SUPRA) still couldn't be written because SUPRA's identity was never resolved. Prevention: for every remaining BUSA League match, `minuteOut` only represents a real substitution when it's strictly less than the match's full-time minute (70) — treat `minuteOut === 70` (or null) as "played to the end," never as a sub-out event. Cross-check against the corresponding `isSub`/`minuteIn` row for the actual incoming player before writing anything.

2026-07-11 (session 41B) — Non-numeric markers in stat cells ("P" for Penalty, "I" for a single card) are silently dropped by any `typeof x === 'number'` truthiness check — caught by Richard reviewing an already-applied match against the real result graphic, not by any internal check. `parse-match-sheet.mjs` preserves whatever the cell literally contained instead of normalizing it, so a goals cell reading "P" (Penalty scored) or a yellow-card cell reading "I" (a single card, tally-mark/OCR artifact — confirmed by Richard to just mean 1) both fail `typeof p[field] === 'number'` and get treated as "no stat," with zero error or warning. Three players (TUNMISE, PAUL, ROLEX in busa-match-15/16) had literally no other numeric field on their row, so they were skipped as players entirely, not just missing one event. Found across 4 already-applied matches (busa-match-13, -16, -15, -14) plus 2 not-yet-written matches (busa-match-11 Prime-Wolves, a Joga-Hammers semifinal sheet) via a full scan of every parsed sheet's stat fields for non-numeric, non-null values. Fixed post-hoc via `dev/fix-missed-markers-and-quantum.mjs`. Prevention: before trusting any `hasAnyStat` / "skip this player" gate anywhere in this backfill, scan every sheet's stat fields for non-numeric non-null values first — a silent skip and a real zero are indistinguishable without this check, and it must be run BEFORE writing, not caught by a human spot-checking the output after the fact.

2026-07-11 (session 41B) — Backfill Substitution events must be ONE paired event per swap, not two unpaired ones — established convention (`backfill-write-md1g2.mjs`'s `addEvent(team, 'Substitution', inName, { relatedName: outName, detail: \`${inName} IN for ${outName}\` })`): `player_id` = the INCOMING player, `related_player_id` = the OUTGOING player, single row, real minute. A from-scratch write script for busa-match-13 instead created two separate unlinked "IN"/"OUT" events per player with no `related_player_id` and no pairing logic — wrong shape, caught only by cross-checking the actual established write script rather than inventing a shape from first principles. Prevention: before writing any new backfill write script, read an existing applied one (`dev/backfill-write-md1g2.mjs` or similar) for the exact event-construction pattern rather than reconstructing conventions from partial context (e.g. a single live-logged event glimpsed elsewhere, which may have its own different authorship path).

2026-07-11 (session 41B) — No single external data source (FA PDF report, sheet, or result graphic) is unconditionally authoritative — each has independently proven wrong at least once this session. FA's structured "Cards" extraction fabricated cards for jerseys that never got one (busa-match-16's Ise/Speedy, busa-match-13's extra Cruise cards) — confirmed wrong because both the source logsheet and a real result graphic agreed against it. A result graphic's own scorer label was wrong once too (busa-match-15's "REWARD," corrected to Kedem by FA's jersey-number cross-reference). Prevention: when adding backfill data from an external source beyond the primary logsheet, cross-check against at least one other independent source before writing — treat a lone source's claim as provisional, and when two sources agree against a third, the third is the error, not a tiebreak to average.

2026-07-11 (session 41B) — Club+college coexistence is mergeable; club+club is not — a firm test for whether two same-named player rows are the same real person. Confirmed via a real case (Abdulazeez Jolaoye/Kings + unassigned "Azeez"/COLNAS-only — merged, same person) vs. two false alarms from the same audit pass (Charles and Peter, each pair holding *active club affiliations to two different clubs* — not mergeable, since only transfers from QF onward are legitimate and both were still group-stage). Prevention: before treating a same-name collision as a duplicate-person candidate, check the *affiliation_type* shape on both sides first — one row club-only + one row college-only (or unassigned) is the mergeable shape; two active club affiliations to different clubs is not, full stop, regardless of name similarity.

2026-07-11 (session 41B) — `players.profile_id` (the cross-sport identity-linking field) is fully built and reachable end-to-end (write path in `getPlayerProfileId()`/bulk-register, read path in `GET /api/players/[id]`, a real rendered "Multi-Sport Athlete" UI card on `/players/[id]`) but has zero live precedent — 0 of 309 players have it populated on this platform as of this session. Its only trigger is two players sharing an email at bulk-register time; individual creation and competition-approval creation both hardcode it to `null`. Not dead code — just never exercised. Relevant before assuming any "multi-sport athlete" feature is either fully working or purely aspirational: verify actual data population, not just code presence, before concluding either way.
